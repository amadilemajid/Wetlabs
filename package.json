{
  "name": "wetlabs-deployment",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "deploy:render": "echo 'Deploying to Render...' && echo 'Push to GitHub to trigger deployment'",
    "deploy:vercel": "cd apps/web && vercel --prod",
    "build:api": "cd apps/api && npm install && npm run build",
    "build:web": "cd apps/web && npm install && npm run build",
    "test:api": "curl https://wetlabs-api.onrender.com/api/v1/health",
    "test:web": "echo 'Open https://wetlabs.vercel.app in browser'"
  }
}
